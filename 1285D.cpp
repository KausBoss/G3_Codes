Drsala(int *ar, int n, int, i, int lim){
	//base case
	if(i==0){
		
		return
	}
}

int main(){
	int n, a[100005], int maxm=0;
	cin>>n;
	for(int i=0; i<n; i++){
		cin>>a[i];
		maxm=max(a[i], maxm);
	}
	int lim=log2(maxm);
}
