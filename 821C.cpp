#include <bits/stdc++.h>

using namespace std;

bool great(int a, int b){
	return a>b;
}

int main(){
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
	stack<int> s;
	s.reserve(3e5 + 5);
	int n, ct=1, ans=0, num;
	string st;
	cin>>n;
	for(int i=0; i<2*n; i++){
		cin>>st;
		if(st == "remove"){
			if(st)
		}
	}
	cout<<ans;
} 